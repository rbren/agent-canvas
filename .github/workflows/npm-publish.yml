name: Publish to npm

on:
  release:
    types: [published]

concurrency:
  group: npm-publish-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js for npm trusted publishing
        uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build library
        run: npm run build:lib

      - name: Verify package contents
        run: npm pack --dry-run

      # Publish using npm's trusted publishing (OIDC-based provenance)
      # This requires configuring the package on npmjs.com to trust this repository.
      # Go to npmjs.com > Package Settings > Publishing access > Add trusted publisher
      # and link this GitHub repository and workflow.
      - name: Publish to npm with provenance
        run: npm publish --access public --provenance
